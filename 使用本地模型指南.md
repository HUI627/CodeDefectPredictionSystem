# 使用本地 CodeBERT 模型 - 快速指南

## 📋 步骤总览

1. **下载模型** → 2. **验证模型** → 3. **使用模型**

---

## 🚀 快速开始

### 步骤 1: 下载模型到本地

运行下载脚本：

```bash
python download_model.py
```

这将自动下载 CodeBERT 模型到 `models/codebert-base/` 文件夹。

### 步骤 2: 验证模型

运行验证脚本确保模型正确安装：

```bash
python verify_local_model.py
```

如果看到 "✓ 所有检查通过！" 说明模型已正确设置。

### 步骤 3: 使用模型

配置文件 `config_example.py` 已经修改为使用本地模型：

```python
self.model_name = str(self.base_dir / "models" / "codebert-base")
```

现在你可以正常运行训练和预测代码，它会自动从本地加载模型。

---

## 📁 目录结构

下载完成后，你的目录结构应该是：

```
D:\研\人工智能\大作业\
├── models/
│   └── codebert-base/          # 本地模型文件夹
│       ├── config.json          # 模型配置
│       ├── pytorch_model.bin    # 模型权重 (~500MB)
│       ├── tokenizer_config.json
│       ├── vocab.txt
│       └── ...
├── src/
│   ├── model.py                 # 会自动使用本地模型
│   └── ...
├── config_example.py            # 已配置为使用本地路径
├── download_model.py            # 下载脚本
├── verify_local_model.py        # 验证脚本
└── LOCAL_MODEL_SETUP.md         # 详细说明文档
```

---

## ⚙️ 配置说明

### 已修改的文件

**config_example.py** (第 11-16 行):
```python
# 使用本地模型路径（将下载的模型放在 models/codebert-base 文件夹中）
self.model_name = str(self.base_dir / "models" / "codebert-base")

# 如果要使用在线模型，取消注释下面这行：
# self.model_name = "microsoft/codebert-base"
```

### 切换回在线模式

如果需要切换回在线下载模式，只需修改配置：

```python
# 注释掉本地路径
# self.model_name = str(self.base_dir / "models" / "codebert-base")

# 使用在线模型
self.model_name = "microsoft/codebert-base"
```

---

## ✅ 优势

使用本地模型的好处：

- ✓ **无需网络**: 离线也能运行
- ✓ **加载更快**: 不需要每次都下载
- ✓ **稳定可靠**: 避免网络问题导致的失败
- ✓ **版本固定**: 确保实验可复现

---

## 🔧 故障排除

### 问题 1: 下载失败

**解决方案**:
- 检查网络连接
- 确保安装了 transformers: `pip install transformers`
- 如果在国内，可能需要配置代理

### 问题 2: 模型加载失败

**解决方案**:
- 运行 `verify_local_model.py` 检查文件完整性
- 确保 `pytorch_model.bin` 文件存在且大小正常（约 500MB）
- 尝试重新下载: `python download_model.py`

### 问题 3: 路径错误

**解决方案**:
- 确保模型文件夹名称为 `codebert-base`
- 检查配置文件中的路径是否正确
- 使用绝对路径或 `Path` 对象

---

## 📚 相关文件

- `LOCAL_MODEL_SETUP.md` - 详细设置指南
- `download_model.py` - 自动下载脚本
- `verify_local_model.py` - 验证脚本
- `config_example.py` - 配置示例（已修改）

---

## 💡 提示

1. 首次下载模型需要约 500MB 空间和几分钟时间
2. 下载完成后，所有训练和预测都会使用本地模型
3. 可以将 `models/codebert-base/` 文件夹备份，方便在其他项目中使用
